# third-party dependencies for Python

The requirements.txt contains the output fomr `pip freeze` after installing Flask in virtualenv, so Flask and it's dependencies, not only Flask itself (important!)